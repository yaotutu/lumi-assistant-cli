# lumi-assistant 直接依赖
# 使用: pip install -r requirements.txt
# 推荐: 使用 uv sync 获得精确版本控制

# 核心音频处理
numpy
sounddevice

# 日志
colorlog

# 阿里云ASR/TTS依赖
aiohttp
websockets
requests

# LLM集成
openai
httpx

# 配置文件解析
PyYAML